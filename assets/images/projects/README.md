# Technical Project Screenshots

Drop project screenshots or cover images here.

Naming convention:
- `project-name.jpg` (e.g., `ml-classifier.jpg`)
- One image per project — grayscale on load, color on hover

Tips:
- Screenshots, mockups, diagrams — anything that represents the project visually
- Add a corresponding entry in work.html for each project
- If a project has no screenshot, use a placeholder with the project name as text
